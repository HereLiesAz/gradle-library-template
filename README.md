# Gradle Library Template

Kotlin/JVM Gradle library starter prepared for Maven publishing to GitHub Packages and compatibility with JitPack.

Repository automation is selected from the central `HereLiesAz/workflows` catalog rather than implemented locally.
